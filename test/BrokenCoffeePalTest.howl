⊐ NUnit.Framework;
⊐ Activ.Rx;

‒ ○  BrokenCoffeePalTest : TestBase {

    ؟ Brew(){
        ∙ brewer = ⌢ BrokenCoffeePal(){
            tap = ⌢ Container(), pot = ⌢ Container(),
            pouch = ⌢ Container(), cup = ⌢ Container(),
            kettle = ⌢ Kettle()
        };
        o( brewer.Brew(), status.done);
    }

    ؟ Pour(){
        ⑂  @out = ⌢ BrokenCoffeePal().Pour(
            ⌢ Container(), ⌢ Container()
        );
        o( @out, status.done );
    }

    ؟ Wait() → o(
        ⌢ BrokenCoffeePal().Wait(minutes: 1),
        status.done
    );

}
